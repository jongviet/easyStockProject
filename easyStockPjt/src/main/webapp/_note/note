drop table if exists member;
drop table if exists stock;
drop table if exists target;
drop table if exists news;
drop table if exists earning;
drop table if exists dailyValue;
drop table if exists account;
drop table if exists board;
drop table if exists comment;

create table member (
email varchar(50) primary key,
pwd varchar(1000) not null,
deposit_usd decimal(10,2) default 50
);

create table stock (
symbol varchar(10) primary key,
fullName varchar(100),
description varchar(2000),
sector varchar(100),
m_capitalization bigint(100),
per decimal(6,2),
eps decimal(6,2),
pxt_insiders decimal(3,2),
pxt_institutions decimal(3,2),
52w_high decimal(6,2),
52w_low decimal(6,2),
avg_target decimal(6,2)
);

create table target (
targetNum int(10) primary key,
symbol varchar(10) not null,
org varchar(100),
targetPrice int(10)
);

create table news (
newsNum int(10) primary key,
symbol varchar(10) not null,
title varchar(1000),
content varchar(15000)
);

create table earning (
earningNum int(10) primary key,
symbol varchar(10) not null,
reportedDate varchar(100),
reportedEPS decimal(6,2),
estimatedEPS decimal(6,2)
);

create table dailyValue (
dvNum int(10) primary key,
date varchar(100),
symbol varchar(10) not null,
open decimal(6,2),
high decimal(6,2),
close decimal(6,2),
d_volume bigint(100)
);

create table account (
h_num int(10) primary key,
email varchar(50) not null,
symbol varchar(10) not null,
avg_h_price decimal(6,2),
h_qty int(10)
);

create table board(
o_id int(10) primary key,
symbol varchar(10) not null,
writer varchar(50) not null,
title varchar(100),
content varchar(2000),
hit int(10),
t_up int(10),
regdate timestamp not null default current_timestamp
);

create table comment(
c_id int(10) primary key,
o_id int(10) not null,
writer varchar(50) not null,
regdate timestamp not null default current_timestamp,
content varchar(500)
);